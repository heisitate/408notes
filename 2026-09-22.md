##  每日总结
1. 完成了二维随机变量分布，接下来是数字特征以及大数定律；
2. 完成了os的io分配方式，还剩余dma方式没完成，接下来完成dma方式和磁盘的内容；
3. 完成了一篇英语阅读，错了三个，感觉确实英一的难度确实比英二难，可能要在单词方面下功夫了，可以新增一个背诵的时间段；
4. 完成了数学的纠错，今天主要是多元微分学以及积分学的证明题；
5. 完成了单词的背诵，感觉不会的单词居多；
6. 今天稍微跑了两圈，晚上吃的有点多了，有点没消化好；
7. 这两天再斟酌斟酌，怎末给大伯发问候消息，哎，感觉不知道从何说起

## 数学总结
1. 


## 算法
```C++
void DFS(int idx){

    if(idx==m+1){
        result.push_back(temp);
        return;
    }
    temp.push_back(idx);
    DFS(idx+1);
    temp.pop_back();
    DFS(idx+1);
}

  

bool cmp(vector<int> &a,vector<int> &b){
    if(a.size() != b.size()){
        return a.size()< b.size();
    }else{
        return a<b;
    }
}
```
