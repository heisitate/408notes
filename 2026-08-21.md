## 每日任务总结

## 408易错总结
1. 红黑树的所有节点都是黑色的，那么他一定是满二叉树。
2. 红黑树的叶子节点都是nil节点，表示为空；
3. 判断m阶非叶节点的子树数量时，首先考虑是否为根节点（至少有两棵子树），然后再根据$[\lceil m-1 \rceil/2]$
4. 高度为h的B树中新插入一个新关键字，
	如果产生了树高度的增加，那么一定产生了根节点的分裂；
	本次因插入而产生的新节点的个数最多为h+1（每层产生一个新节点，然后＋一个新根节点）；
	本次操作产生的读/写磁盘次数最多为3h+1(读磁盘n次，写磁盘：写回原节点+写入新节点（每层2次写)+根分裂（1次））；
5. 