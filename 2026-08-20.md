## 408每日总结
1. 红黑树和平衡二叉树两者查找，插入，删除的时间复杂度相同；红黑树的插入至多需要2次，删除至多需要三次；
## 数学总结
1. 今天看课感觉不如自己学；少看课，只看知识部分，其余例题自己写；
2. 睡眠时间没有控制好，导致早上起来迟了，尽量早睡；
## 每日算法总结
1. 寻找第一个大于x的数的下标
```C
   while(low<high){
         mid=(low+high)/2;
        if(a[mid] > x){
            high = mid;
        }else{
            low = mid+1;
        }
    }
```
2. 寻找封顶元素：
```C
   int BiSearch(int a[]){
    int low=0;
    int high=n;
    int mid;
    while(low<high){
         mid=(low+high)/2;
        if(a[mid] >= a[mid-1]){
            low = mid+1;
        }else{
            high = mid;
        }
    }
    return low-1;
}
```
## 学习总结

408知识点整理实在是太多了，该怎末进行复习才比较高效可靠呢？；
我实在不能每天抄笔记了......这不算在学习emmmm.......
## 英语每日总结
exert vt 行使
discourse  n 言谈
intimate adj 亲密的隐私的
be absorbed in 融入
bilingual adj 双语的
indices n 指标
还是要多背例句，体会其中的语义