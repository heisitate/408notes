## 408知识总结
1. 局部性原理是指程序在执行时并非随机，均匀地访问整个地址空间，而是呈现出聚簇地特征。
2. 地址映射的逻辑规则与管理由操作系统完成的，具体的硬件加速由mmu完成；
3. Cache缺失后的处理完全由硬件处理，但是TLB缺失后的处理根据不同的架构由不同的结构处理（x86完全由硬件处理，Mips架构则是由操作系统软件处理）；
4. 分段是按程序的逻辑单位（如函数段，数据）划分的，每个段具有完整的逻辑意义，跟有利于设置访问权限；
5. 分页是物理上的硬性划分，一个页面内可能既包含代码又包含数据，难以进行精细化的逻辑保护。
6. 页表通常放在主存的内核空间，为了保证系统的安全与稳定，用户进程无权直接访问页表，页表的维护与地址转换（通过mmu）由操作系统和硬件配合完成。
